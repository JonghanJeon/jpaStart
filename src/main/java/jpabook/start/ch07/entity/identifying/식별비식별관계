식별 관계
    ㄴ> 부모 테이블 의 기본 키를 내려 받아서 자식 테이블 의 기본 키 + 외래 키로 사용 하는 관계
    ㄴ> 구현 하기 개빡셈
    ㄴ> 식별 관계로 하면 웬만하면 복합키가 사용이 되게 되는데 이때 복합 키를 구현하는게 좀 빡셈. (클래스를 따로 만들어줘서 연결해줘야함)
    ㄴ> 식별 관계는 보통 비즈니스 의미가 있는 자연 키 컬럼을 조합하는 경우가 많음
        => 비즈니스 요구사항이 변하게 되면, 자식에 손자까지 전파되기 때문에 변경하기 힘들다.

비식별 관계
    ㄴ> 부모 테이블 의 기본 키를 받아서 자식 테이블 의 외래 키로만 사용 하는 관계

데이터 베이스 테이블을 설계할 때 식별 관계나 비식별 관계 중 하나를 선택해야 한다.
    ㄴ> 최근에는 비식별 관계를 주로 사용하고 꼭 필요한 곳에만 식별 관계를 사용하는 추세이다.
    ㄴ> JPA 는 식별 관계와 비식별 관계를 모두 지원한다.